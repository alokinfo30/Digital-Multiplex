# Dockerfile.libretranslate
FROM libretranslate/libretranslate:latest

# The server will run on port 5000 inside the container.
# Render will handle the external port mapping.
CMD ["--host", "0.0.0.0", "--port", "5000", "--load-only", "en,hi,es,fr,de,zh,ja,ko,pt,ru,it"]
